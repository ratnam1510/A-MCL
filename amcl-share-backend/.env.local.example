# Vercel Blob storage token. Create one at
# https://vercel.com/dashboard/stores → Blob → "Read & Write Token".
BLOB_READ_WRITE_TOKEN=

# Set automatically by `vercel dev` / `vercel deploy`. Leave empty locally.
VERCEL_OIDC_TOKEN=
